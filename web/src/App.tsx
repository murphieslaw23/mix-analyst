Replace the malformed Phase 15 App.tsx integration with a compiling version that: renders an audio element with selectedMix.audio_url; creates the analyser graph only after the media element exists; synchronizes play/pause, seeking, timeupdate, and ended events; handles autoplay rejection; maps MIDI CC values to visualizer settings and actual audio volume; maps MIDI note actions to debounced play/pause and previous/next cue navigation; retains the existing analyzer, support, and imprint UI.